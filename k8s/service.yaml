apiVersion: v1
kind: Service
metadata:
  name: chatbot-service
  labels:
    app: chatbot
spec:
  type: LoadBalancer # Expose externally via cloud provider's load balancer
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
  selector:
    app: chatbot
